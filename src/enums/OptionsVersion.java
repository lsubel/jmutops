package enums;
/**
 * Enumeration used in case of adding classpath / sourcepath to distinguish between prefixed and postfixed code. 
 * @author Lukas Subel
 *
 */
public enum OptionsVersion {
	PREFIX, POSTFIX;
}